
mixin sg-header(data)
  -
    data.title = data.title || 'styleguide';
    data.linkList = data.linkList || [];

  .sg-header
    h1.sg-header_title= data.title

    if (data.linkList.length)
      +sg-tabs(data.linkList).sg-header_tabs
